<div class="container">
  <div class="row">
    <div class="col-md-8 topic-area">
      <% if @topics.any? %>
        <ol class="topics">
          <%= render @topics %>
        </ol>
         <%= paginate @topics %>
      <% else %>
        トピックはまだありません
      <% end %>
    </div>
    <div class="col-md-4">
      <%= render "shared/category_index", categories: @categories %>
    </div>
  </div>
</div>